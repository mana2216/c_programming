#include <stdio.h>

int main(void)
{
    int hour = 24 * 365;
    int minute = 60 * 24 * 365;
    int seconds = 60 * 60 * 24 * 365;
    printf("hour = %d\n", hour);
    printf("minute = %d\n", minute);
    printf("seconds = %d\n", seconds);
}